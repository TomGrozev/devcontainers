name: Build and Push

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Explicit semver release tag to publish (optional)"
        type: string
        required: false
        default: ""
  workflow_call:
    inputs:
      release_tag:
        description: "Explicit semver release tag to publish (overrides tag detection)"
        type: string
        required: false
        default: ""

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.release_tag || github.ref }}

      - name: Extract version tag
        id: version
        run: |
          if [[ -n "${{ inputs.release_tag }}" ]]; then
            tag="${{ inputs.release_tag }}"
          elif [[ "${{ github.ref_type }}" == "tag" ]]; then
            tag="${{ github.ref_name }}"
          else
            tag=""
          fi
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"

          # Actual checked-out commit (differs from github.sha when release_tag
          # pins the build to a bot tag off the master head).
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/bake-action@v7
        env:
          VERSION: ${{ steps.version.outputs.tag }}
          GIT_SHA: ${{ steps.version.outputs.sha }}
          CREATED: ${{ github.event.head_commit.timestamp || github.event.repository.updated_at }}
        with:
          source: .
          targets: all
          push: true
